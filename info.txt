/* https:// (domain)/api/(endpoint)
https requests has verbs or meta (intentions)
    GET getting data
        GET /customers get all 
            GET /customers/1 ( as ID) only that customer
    POST create data
        Include object
    PUT edite data    
        Include the object and ID

    DElETE delete data

*/
not maintanable but works
/*
const https = require (`https`)

const server = htpps.createServer((req,res)=>{
    if (req.url ===''){
        res.write(`Hello World`)
        res.end()
    }
})

server.listen(3000)

console.log('Listening on port 3000...') 
*/